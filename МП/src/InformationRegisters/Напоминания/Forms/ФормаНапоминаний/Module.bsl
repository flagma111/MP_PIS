
&НаКлиенте
Процедура ЗакрытьФорму(Команда)//Ден  2016 10 11 ---
	Закрыть();
КонецПроцедуры

&НаКлиенте
Процедура СписокНапоминанийВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)//Ден  2016 10 11 ---
	ТекущиеДанные = Элемент.ТекущиеДанные;	
	Структура = Новый Структура("Ключ", ПолучитьКлюч(ТекущиеДанные.Элемент, ТекущиеДанные.Комментарий));
	ОткрытьФорму("РегистрСведений.Напоминания.Форма.ФормаЗаписи", Структура);

	ИндексТекущейСтроки = СписокНапоминаний.Индекс(ТекущиеДанные);
	СписокНапоминаний.Удалить(ИндексТекущейСтроки);
	Если СписокНапоминаний.Количество() = 0 Тогда
		закрыть();
	КонецЕсли;
КонецПроцедуры

&НаСервере 
Функция ПолучитьКлюч(Элемент, Комментарий)//Ден  2016 10 27 ---
	Возврат РегистрыСведений.Напоминания.СоздатьКлючЗаписи(Новый Структура("Элемент, Комментарий", Элемент, Комментарий));
КонецФункции

//&НаСервере
//Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)//Ден  2016 10 14
//	ЗаполнитьСписокНапоминаний();	
//КонецПроцедуры

&НаСервере
Процедура ЗаполнитьСписокНапоминаний()//Ден  2016 10 14
	
	СписокНапоминаний.Очистить();
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Напоминания.ДатаНапоминания КАК ДатаНапоминания,
	|	Напоминания.Элемент КАК Элемент,
	|	Напоминания.Комментарий КАК Комментарий,
	|	Напоминания.ВремяНапоминания КАК ВремяНапоминания,
	|	Напоминания.Периодичность КАК Периодичность
	|ИЗ
	|	РегистрСведений.Напоминания КАК Напоминания
	|ГДЕ
	|	Напоминания.ДатаНапоминания <= &ДатаНапоминания";	
	Запрос.УстановитьПараметр("ДатаНапоминания", ТекущаяДата());	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Нстр = СписокНапоминаний.добавить();
		Нстр.Напоминание = строка(Выборка.ДатаНапоминания) + " " + Выборка.Элемент + " " + Выборка.Комментарий; 
		ЗаполнитьЗначенияСвойств(Нстр,Выборка);
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаАктивизации(АктивныйОбъект, Источник)//Ден  2016 10 14
	ЗаполнитьСписокНапоминаний();	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)//Ден  2016 10 14
	Если ИмяСобытия = "ПерезаписьНапоминаний" Тогда
		ЗаполнитьСписокНапоминаний();
		Если СписокНапоминаний.Количество() = 0 Тогда
			Закрыть();
		КонецЕсли;
	КонецЕсли;	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ЗаполнитьСписокНапоминаний();	
КонецПроцедуры





